import React from "react";

import styles from "./page.module.scss";

const Shop: React.FC = () => {
  return (
    <main>
      <h2 className={styles.heading}>Shop</h2>
    </main>
  );
};

export default Shop;
